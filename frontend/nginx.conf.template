server {
    listen ${PORT};
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA: unknown paths fall back to index.html (same behavior as vercel.json's rewrite)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
